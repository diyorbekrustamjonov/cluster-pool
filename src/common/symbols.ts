export namespace Symbols {
  export namespace Task {
    export const kQueueOptions = Symbol.for('NovaPool.queueOptions');
    export const kTask = Symbol.for('NovaPool.task');
    export const kValue = Symbol.for('NovaPool.valueOf');
    export const kMovable = Symbol.for('NovaPool.kMovable');
    export const kTransferable = Symbol.for('NovaPool.transferable');
  }
}
